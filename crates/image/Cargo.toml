[package]
name = "image"
version = "0.1.0"
edition = "2024"

[dependencies]
object = { version = "0.36", default-features = false, features = ["elf", "read"] }
zerocopy = { version = "0.8.47", features = ["derive"] }
