[package]
name = "image"
version = "0.1.0"
edition = "2024"

[dependencies]
idb-rs = { git = "https://github.com/ioncodes/idb-rs.git" }
object = { version = "0.36.7", default-features = false, features = ["elf", "read"] }
zerocopy = { version = "0.8.48", features = ["derive"] }
