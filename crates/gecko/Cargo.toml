[package]
name = "gecko"
version = "0.1.0"
edition = "2024"

[features]
scripting = []
scripting-mut-traps = ["scripting"]

[build-dependencies]
chipi = "0.5.3"

[dependencies]
chapa = { git = "https://github.com/ioncodes/chapa.git" }
tracing = { version = "0.1.44", features = ["release_max_level_off"] }
image = { path = "../image" }
