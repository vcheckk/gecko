[package]
name = "tinybench"
version = "0.1.0"
edition = "2024"

[features]
idle-skip = ["gecko/idle-skip"]

[dependencies]
gecko = { path = "../gecko" }
image = { path = "../image" }
clap = { version = "4.6.0", features = ["derive"] }
