[package]
name = "scripting"
version = "0.1.0"
edition = "2024"

[features]
scripting-mut-traps = ["gecko/scripting-mut-traps"]

[dependencies]
gecko = { path = "../gecko", features = ["scripting"] }
mlua = { version = "0.10", features = ["lua54", "send"] }
tracing = "0.1"
